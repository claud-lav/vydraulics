{% set sections = section.reusableBlock.one().contentBuilder.all() %}

{% for section in sections %}
    {% include "_blocks/" ~ section.type.handle|kebab with {
        index: index
    }%}
{% endfor %}